{% extends 'base.html.twig' %}

{% block title %}Modifier le Tag{% endblock %}

{% block body %}
<h1 class="text-2xl font-bold mb-4">Modifier le Tag</h1>

{{ form_start(tagForm, {'attr': {'class': 'space-y-4'}}) }}
    {{ form_widget(tagForm, {'attr': {'class': 'w-full p-2 border border-gray-300 rounded'}}) }}
    <button type="submit" class="btn btn-primary bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Enregistrer</button>
{{ form_end(tagForm) }}

<a href="{{ path('app_admin_interface') }}" class="btn btn-secondary bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600">Retour à la liste</a>
{% endblock %}
